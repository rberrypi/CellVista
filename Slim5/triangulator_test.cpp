#include "stdafx.h"
/*
#include "stdafx.h"
#include "cgal_triangulator.h"
#include <iostream>

void triangulator_test()
{
	cgal_triangulator triangulation;
	auto src = scope_location_xyz(0, 0, 0);
	triangulation.insert_point(src);
	triangulation.insert_point(src);
	triangulation.insert_point(src);
	triangulation.insert_point(src);
	triangulation.move_point(src,scope_location_xyz(1, 0, 0));
	triangulation.move_point(src,scope_location_xyz(1, 1, 1));
	triangulation.move_point(src,scope_location_xyz(0, 1, 1));
	std::vector<scope_location_xyz> query_points = { scope_location_xyz(0.5,0.5,1)};
	triangulation.interpolate(query_points);
	std::cout << query_points.front().z << std::endl;
}
*/